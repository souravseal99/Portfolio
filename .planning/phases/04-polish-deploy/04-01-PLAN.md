---
phase: 04-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .vercel
autonomous: false
requirements:
  - PERF-01
  - PERF-02
  - SEO-01
  - SEO-02

must_haves:
  truths:
    - "Production build succeeds without errors"
    - "Site is deployed and accessible at public URL"
    - "Page loads in under 3 seconds on desktop (Lighthouse)"
    - "SEO meta tags visible in page source"
    - "Open Graph tags present for social sharing"
  artifacts:
    - path: ".vercel"
      provides: "Vercel deployment config"
    - path: "src/app/layout.tsx"
      provides: "SEO metadata (already implemented)"
      contains: "metadata: Metadata"
  key_links:
    - from: "layout.tsx"
      to: "Browser"
      via: "Static HTML generation"
      pattern: "export const metadata"
---

<objective>
Complete Phase 4 Polish & Deploy: verify production build, deploy to Vercel, and confirm performance meets requirements (page load < 3 seconds).
</objective>

<execution_context>
@/Users/souravseal/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/souravseal/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-deploy/04-RESEARCH.md

## Current Status

- SEO metadata already implemented in src/app/layout.tsx (title, description, Open Graph, Twitter cards)
- No images in codebase - next/image optimization not applicable
- Build was previously tested (commit 487652c)

## Deployment Options

1. **Vercel CLI:** Run `npx vercel login` then `npx vercel --prod`
2. **GitHub Integration:** vercel.com → Add New → Import GitHub repo
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify production build succeeds</name>
  <files>package.json</files>
  <action>
    Run `npm run build` to verify the production build works without errors. This ensures:
    - TypeScript compilation passes
    - Next.js build completes
    - Static pages generate correctly
    - No build-time errors
  </action>
  <verify>npm run build completes with exit code 0</verify>
  <done>Production build succeeds with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel</name>
  <files>.vercel</files>
  <action>
    Deploy the site to Vercel using one of these methods:

    **Option A - Vercel CLI (if authenticated):**
    ```bash
    npx vercel --prod
    ```

    **Option B - GitHub Integration:**
    1. Push code to GitHub (if not already)
    2. Go to vercel.com
    3. Add New → Project → Import GitHub repository
    4. Deploy with default settings

    After deployment, note the production URL.
  </action>
  <verify>Site accessible at [vercel-project].vercel.app</verify>
  <done>Site deployed and publicly accessible</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Full portfolio deployed to production</what-built>
  <how-to-verify>
    1. Visit the deployed Vercel URL
    2. Open Chrome DevTools → Lighthouse tab
    3. Run Lighthouse analysis (Mobile or Desktop)
    4. Verify Performance score
    5. Check SEO score
    
    **Success criteria:**
    - Performance: > 90 (page loads < 3 seconds)
    - SEO: > 90
  </how-to-verify>
  <resume-signal>Type "approved" with Lighthouse scores, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Production build passes without errors
- Site deployed to Vercel
- Lighthouse Performance score > 90
- Lighthouse SEO score > 90
- All 4 requirements (PERF-01, PERF-02, SEO-01, SEO-02) satisfied
</verification>

<success_criteria>
- Production build: `npm run build` succeeds
- Deployment: Site accessible at public Vercel URL
- Performance: Lighthouse confirms < 3 second load time
- SEO: Meta tags present in page source
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-deploy/04-01-SUMMARY.md`
</output>
