---
phase: 03-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/layout.tsx]
autonomous: true
requirements: [SEO-01, SEO-02, PERF-02]
user_setup: []

must_haves:
  truths:
    - "Meta title 'Sourav Seal - Full-Stack Developer' is present in page source"
    - "Meta description is present describing full-stack developer"
    - "Open Graph tags (og:title, og:description, og:url) are present for social sharing"
    - "All images use next/image component with proper alt text"
    - "Images automatically serve WebP format and lazy load"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Global SEO metadata and Open Graph tags"
      exports: "Metadata object with title, description, openGraph"
  key_links:
    - from: "src/app/layout.tsx"
      to: "Browser <head>"
      via: "Next.js Metadata API"
      pattern: "export.*metadata.*Metadata"
---

<objective>
Add SEO metadata and optimize all images for performance.
</objective>

<context>
@.planning/phases/03-polish-deploy/03-RESEARCH.md

Next.js Metadata API pattern:
```tsx
export const metadata: Metadata = {
  title: 'Sourav Seal - Full-Stack Developer',
  description: 'Full-Stack Developer at CloudKaptan with 3+ years experience building scalable web applications.',
  openGraph: { ... },
};
```

next/image pattern:
```tsx
<Image src="/profile.jpg" alt="Sourav Seal" width={200} height={200} />
```
</context>

<tasks>

<task type="auto">
  <name>Add SEO metadata to layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <action>
    Add Metadata import from 'next' and export metadata object with:
    - title: 'Sourav Seal - Full-Stack Developer'
    - description: 'Full-Stack Developer at CloudKaptan with 3+ years experience building scalable web applications using React, Next.js, Node.js, and cloud technologies.'
    - openGraph: { title, description, url: 'https://souravseal.vercel.app', siteName: 'Sourav Seal Portfolio', locale: 'en_US', type: 'website' }
    
    Reference: https://nextjs.org/docs/app/api-reference/functions/generate-metadata
  </action>
  <verify>
    Run: npm run build && grep -r "Sourav Seal - Full-Stack Developer" .next/server
    Should find metadata in build output
  </verify>
  <done>
    Page source shows: <title>Sourav Seal - Full-Stack Developer</title> and <meta name="description" content="...">
  </done>
</task>

<task type="auto">
  <name>Convert images to next/image for optimization</name>
  <files>src/app/page.tsx</files>
  <action>
    Find all <img> tags in page.tsx and replace with next/image:
    1. Import Image from 'next/image'
    2. Replace <img src="..." alt="..."> with <Image src="..." alt="..." width={X} height={Y} />
    3. For profile image in About section: width={300} height={300}
    4. For project images: use appropriate dimensions based on card size
    
    This automatically enables:
    - WebP format conversion
    - Lazy loading (default)
    - Layout shift prevention
    
    If profile image is a placeholder, create /public/profile.jpg with actual image or keep as placeholder with next/image
  </action>
  <verify>
    Run: npm run build && grep -c "next/image" .next/server/app/*.html
    Verify Image component is used in build output
  </verify>
  <done>
    All images use next/image component, images will auto-optimize to WebP with lazy loading
  </done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds without errors
- [ ] Build output contains metadata in page head
- [ ] All img tags replaced with Image component
</verification>

<success_criteria>
PERF-02: Images optimized (WebP, lazy loading) ✓
SEO-01: Meta title "Sourav Seal - Full-Stack Developer" + description ✓
SEO-02: Open Graph tags present ✓
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-deploy/03-01-SUMMARY.md`
</output>
