---
phase: 03-ui-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - src/components/sections/Hero.tsx
  - src/components/sections/About.tsx
  - src/components/sections/Skills.tsx
  - src/components/sections/Contact.tsx
  - src/components/sections/Experience.tsx
  - src/components/sections/Projects.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "Color palette matches Colorhunt palette (cream #fef5ed, sage green #d3e4cd, taupe #adc2a9, muted purple #9a7999)"
    - "Typography matches alexandretrotel.org style (clean, readable, proper line-heights)"
    - "All sections use new color palette consistently"
    - "Witty developer lines appear throughout sections"
    - "Tech jargon updates applied (Send Request, Tech Arsenal, System Overview)"
    - "Color contrast meets WCAG AA minimum (4.5:1 for text)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Color palette CSS variables"
      contains: "--color-cream, --color-sage, --color-taupe, --color-muted-purple"
    - path: "src/app/layout.tsx"
      provides: "Typography font configuration"
      contains: "Inter font from next/font/google"
    - path: "src/components/sections/*.tsx"
      provides: "All section components with new styling"
      min_sections: 6
  key_links:
    - from: "globals.css"
      to: "All section components"
      via: "CSS variables"
      pattern: "var\\(--color-"
---

<objective>
Redesign Phase 3 UI to address new requirements: (1) typography from alexandretrotel.org, (2) new Colorhunt color palette replacing purple/magenta/pink, (3) more witty developer lines, (4) tech jargon updates, (5) improved contrast for WCAG AA compliance.
</objective>

<execution_context>
@/Users/souravseal/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/souravseal/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-enhancement/CONTEXT.md

## Redesign Requirements (from user):

### Typography Inspiration
- Source: https://www.alexandretrotel.org/
- Key characteristics: Clean sans-serif (Inter-like), smaller text sizes, excellent readability, good line-heights (1.6-1.7), subtle elegant design

### New Color Palette (Colorhunt)
- #fef5ed (cream) - Background
- #d3e4cd (sage green) - Accents, buttons
- #adc2a9 (taupe) - Secondary elements  
- #9a7999 (muted purple) - Gradients, highlights

### Tech Jargon Updates
| Original | New Version |
|----------|-------------|
| Contact | "Send Request" or "Initialize Connection" |
| Skills | "Tech Arsenal" or "Stack" |
| About Me | "System Overview" |

### Witty Developer Lines (add more throughout)
- `// TODO: write better bio`
- `git commit -m "const future = await buildAmazingThings()"`
- `console.log("Hello, World!"); // My kind of greeting`

### Contrast Requirements
- WCAG AA minimum: 4.5:1 for normal text
- Ensure readable text on all backgrounds
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update global CSS with new Colorhunt color palette</name>
  <files>src/app/globals.css</files>
  <action>
    Replace the existing purple/magenta/pink color palette in globals.css with the new Colorhunt palette:

    Add these CSS variables:
    ```css
    --color-cream: #fef5ed;
    --color-sage: #d3e4cd;
    --color-taupe: #adc2a9;
    --color-muted-purple: #9a7999;
    ```

    Update the :root section to use cream as background (#fef5ed) and update foreground/colors for proper contrast. Ensure text colors have sufficient contrast (minimum 4.5:1 ratio with background).

    Also update dark mode colors to maintain the palette aesthetic while ensuring readability.
  </action>
  <verify>grep -E "(cream|sage|taupe|muted-purple)" src/app/globals.css</verify>
  <done>globals.css contains all 4 Colorhunt colors as CSS variables</done>
</task>

<task type="auto">
  <name>Task 2: Update typography to match alexandretrotel.org style</name>
  <files>src/app/layout.tsx</files>
  <action>
    Replace Geist font with Inter from next/font/google to match alexandretrotel.org clean typography style:

    1. Import Inter (not Geist): `import { Inter, JetBrains_Mono } from "next/font/google";`
    2. Configure Inter with proper weights (400, 500, 600, 700)
    3. Set CSS variable: `--font-inter: var(--font-inter)`
    4. Update body class to use Inter
    5. Consider updating global styles for better line-height (1.6-1.7 for readability)

    Keep JetBrains Mono for code/terminal elements.
  </action>
  <verify>grep -E "Inter" src/app/layout.tsx && npm run build 2>&1 | head -20</verify>
  <done>Inter font loaded and applied to body, build succeeds</done>
</task>

<task type="auto">
  <name>Task 3: Update Hero section with new colors and typography</name>
  <files>src/components/sections/Hero.tsx</files>
  <action>
    Update Hero.tsx with:

    1. Replace purple gradient background with sage green to muted purple gradient
    2. Update name text to use new muted-purple color or keep dark for contrast
    3. Update tagline styling with better typography (smaller, better line-height)
    4. Change CTA button to use sage green (#d3e4cd) as background with good contrast text

    Example gradient: `bg-gradient-to-r from-[#9a7999] to-[#adc2a9]`
    Button: `bg-[#d3e4cd] text-gray-900 hover:bg-[#adc2a9]`
  </action>
  <verify>grep -E "(sage|taupe|muted-purple|cream)" src/components/sections/Hero.tsx</verify>
  <done>Hero section uses new color palette with good contrast</done>
</task>

<task type="auto">
  <name>Task 4: Update About section with tech jargon and witty lines</name>
  <files>src/components/sections/About.tsx</files>
  <action>
    Update About.tsx with:

    1. Change "About Me" header to "System Overview" (tech jargon)
    2. Update terminal-style header colors from purple to muted-purple (#9a7999)
    3. Update gradient accent to use sage→taupe or muted-purple→taupe
    4. Add more witty developer lines throughout the bio paragraphs:
       - `// TODO: write better bio` 
       - `git commit -m "const future = await buildAmazingThings()"`
       - Keep existing lines but add these subtly in the bio text
    5. Update profile placeholder background to use sage green gradient
    6. Ensure all text meets WCAG AA contrast (4.5:1)
  </action>
  <verify>grep -E "(System Overview|TODO|git commit)" src/components/sections/About.tsx</verify>
  <done>About section has System Overview title and multiple witty lines</done>
</task>

<task type="auto">
  <name>Task 5: Update Skills section with Tech Arsenal jargon</name>
  <files>src/components/sections/Skills.tsx</files>
  <action>
    Update Skills.tsx with:

    1. Change section title from "Skills" to "Tech Arsenal" (or "Stack")
    2. Update terminal header color from purple to muted-purple (#9a7999)
    3. Update gradient accent to use new palette
    4. Change badge backgrounds from blue to sage green or taupe tones
    5. Add a witty line: `// Skills: loaded successfully`
  </action>
  <verify>grep -E "(Tech Arsenal|Stack|loaded successfully)" src/components/sections/Skills.tsx</verify>
  <done>Skills section renamed to Tech Arsenal with new colors</done>
</task>

<task type="auto">
  <name>Task 6: Update Contact section with Send Request jargon</name>
  <files>src/components/sections/Contact.tsx</files>
  <action>
    Update Contact.tsx with:

    1. Change section title from "Contact" to "Send Request" or "Initialize Connection"
    2. Update terminal header color from purple to muted-purple
    3. Update gradient accent to use new palette
    4. Update icon backgrounds from blue to sage green
    5. Add witty line: `// Ready to collaborate!`
  </action>
  <verify>grep -E "(Send Request|Initialize Connection|Ready to collaborate)" src/components/sections/Contact.tsx</verify>
  <done>Contact section renamed to Send Request with new colors</done>
</task>

<task type="auto">
  <name>Task 7: Update remaining sections (Experience, Projects) with new palette</name>
  <files>src/components/sections/Experience.tsx, src/components/sections/Projects.tsx</files>
  <action>
    Update Experience.tsx and Projects.tsx with:

    1. Update terminal header colors from purple to muted-purple
    2. Update gradient accents to use new palette (sage→taupe or muted-purple→taupe)
    3. Add witty developer lines appropriate to each section:
       - Experience: `// printf("Learning since %s", "2019");`
       - Projects: `git checkout projects // Browse my work`
    4. Ensure proper contrast throughout
  </action>
  <verify>grep -E "(printf|git checkout)" src/components/sections/Experience.tsx src/components/sections/Projects.tsx</verify>
  <done>Experience and Projects sections updated with new colors and witty lines</done>
</task>

<task type="auto">
  <name>Task 8: Verify contrast and WCAG AA compliance</name>
  <files>src/app/globals.css, src/components/sections/*.tsx</files>
  <action>
    Final verification task:

    1. Review all text colors against backgrounds to ensure 4.5:1 contrast ratio
    2. Check that cream background (#fef5ed) has dark enough text (not gray-500 or lighter)
    3. Verify all gradient accents use the new palette
    4. Run browser devtools or check manually that text is readable
    5. Fix any remaining contrast issues

    Key contrast checks:
    - Body text on cream: Use dark gray-800 or gray-900, NOT gray-600 or lighter
    - Muted text: minimum gray-600 on cream
    - Buttons: Ensure text has sufficient contrast against button background
  </action>
  <verify>Manual verification: Open localhost:3000 and verify text readability</verify>
  <done>All sections pass visual contrast check</done>
</task>

</tasks>

<verification>
- All 4 Colorhunt colors applied consistently across sections
- Inter font configured and loaded
- Typography matches alexandretrotel.org clean style
- Tech jargon updates: System Overview, Tech Arsenal, Send Request
- Witty lines appear in multiple sections
- Contrast passes WCAG AA (4.5:1 minimum)
</verification>

<success_criteria>
- Build succeeds without errors
- All sections use new color palette
- Typography updated to Inter with proper line-heights
- Tech jargon applied (System Overview, Tech Arsenal, Send Request)
- At least 5 witty developer lines across sections
- Color contrast meets WCAG AA
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-enhancement/03-01-SUMMARY.md`
</output>
